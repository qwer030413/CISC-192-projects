#include <iostream>
#include <string>
#include <fstream>
#include <math.h>       
#include <bits/stdc++.h>

using namespace std;

void simplify(int num, int den, int which)
{
   //for loop
   //if thing

    //if which == 1
    //str.numerator1 = num
    //str.denominator1 = den
    

    //else if(which == 2)
    //str.numerator2 = num
    //str.denominator2 = den
}
int main(){

cout << "chris"+ "park";
}